---
name: bjxh-ppt
description: 北京兴华会计师事务所内部 PPT 制作规范 — 基于 2026 合伙人大会精简版模板（8 页样例 + 111 个母版版式），中约束、.pptx 直出、Markdown 大纲输入。Use when the user asks for "按北京兴华规范做 PPT"、"兴华模板"、"公司统一 PPT 样式"、"审计/合伙人大会/工作汇报 PPT"、"兴华风格 PPT"、"内部汇报演示稿"。
---

# 北京兴华 PPT 制作规范 v1.2

> **模板来源**：`C:\工作\04-总结与报告\2026年工作\2026合伙人大会\北京兴华模板.pptx`（20MB · 8 页样例 · 111 个版式）
>
> **数据校验**：本规范的画布/字体/字号/版式数量均已通过 `scripts/dump_template_spec.py` 实际读取模板验证，详见同目录 `template_spec.json`。
>
> **执行档位**：中约束 —— 锁核心规范（画布/字体/字号/颜色/行距/常用版式），其它细节（图表样式、动效）允许按内容发挥。

---

## 快速开始（同事视角）

**最常用流程 — Markdown 大纲 → 规范的 .pptx：**

```bash
# 0. 首次使用：配置模板路径
cp scripts/config.example.json scripts/config.json
#    编辑 config.json，填入 template_path（或设置 BJXH_TEMPLATE 环境变量）

# 1. 在 examples/audit_demo.md 风格写自己的大纲
# 2. 一行命令生成
python scripts/from_outline.py my_outline.pptx.md my_output.pptx
# 3. 自检
python scripts/qa.py my_output.pptx
# 4. PowerPoint 打开微调版式 / 配色 / 图表
# 5. 再跑一次 qa.py 确认
```

**大纲语法速查：**

```markdown
# 演示主标题                      → 自动选 主题-封面

## 章节名                          → 自动选 标题页-空白（章节封面）
### 页面标题                        → 默认 文字模板1
- 要点 1
- 要点 2
> layout: 无图分段-3项              → 覆盖默认版式
> note: 演讲者备注                  → 加备注
```

> **紧凑模式**：如果希望 `## ` 直接作为内容页（不要章节封面页），例如 `封面 + 目录 + 3页内容 + 封底 = 6 页`，可在生成时加 `--no-chapter-covers`。此时 H2 标题同时作为目录项和正文页标题，H2 下需要直接带 bullet。

详见 `examples/audit_demo.md` 完整样例。

**典型陷阱 & 解法：**

| 同事常见问题 | 解决 |
|---|---|
| "我用了宋体,QA 没报错" | qa.py 报的是缺失值,字体名写错会逃过检查。**人工肉眼检查 + PowerPoint 的"替换字体"功能** |
| "全是文字模板1,QA 报 THIN-PAGE" | 文字模板1 适合 1-2 行。多 bullet 用 `无图分段-3项/4项/5项`,数据用 `图表-1~8` |
| "我想加图表/形状" | 在 PowerPoint 里手动加,qa.py 不会阻止 |
| "模板里找不到 `文字模板26`" | 模板实际只有 25 个文字模板,见 `template_spec.json` |
| "封面太大/太小" | 字号 36pt 写死,要调整就 `Pt(40)` 等,保留微软雅黑 |
| "忘了删模板里的 8 页样例" | from_outline.py 已自动清空模板原页,无需处理 |

| 想做 6 页以内的紧凑汇报 | 用 `--no-chapter-covers`，让 H2 直接当内容页，目录仍会自动生成 |

**目录速查：**

- 规范细节 → §一-§八
- 工具脚本 → §九
- 工作流 → §十
- 禁止事项 → §十一

---

## 一、画布

| 项 | 值 |
|---|---|
| 宽 | **13.333 英寸**（12192000 EMU） |
| 高 | **7.5 英寸**（6858000 EMU） |
| 比例 | 16:9 宽屏 |

代码（python-pptx）：
```python
prs = Presentation("北京兴华模板.pptx")
# 画布尺寸直接继承自模板，无需修改
assert prs.slide_width == 12192000 and prs.slide_height == 6858000
```

---

## 二、字体

| 语言 | 字体 | python-pptx |
|---|---|---|
| 中文 | **微软雅黑**（Microsoft YaHei） | `run.font.name = 'Microsoft YaHei'` |
| 英文/数字 | **Arial** | `run.font.name = 'Arial'` |

**规则**
- 模板实际字体为 `+mn-ea`（Office 东亚主字体别名）渲染为微软雅黑；为避免歧义，**代码生成时显式写 `'Microsoft YaHei'`**
- 中英混排：整体设为微软雅黑，英文部分自动 fallback 到 Arial
- **禁止使用** 宋体、黑体、楷体、隶书、华文、Times New Roman、Calibri 等

---

## 三、字号

下表来自模板前 30 页真实 run 统计（见 `template_spec.json`），按使用频率排序：

| 层级 | 字号 | python-pptx | 用途 |
|---|---|---|---|
| 封面主标题 | **28pt** | `Pt(28)` | 封面核心标题（实测 36pt 装不下 20+ 字长标题） |
| 章节标题 | **24pt** | `Pt(24)` | 章节封面（实测 32pt 溢出 3.87×0.54 寸框） |
| 页面主标题 | **20pt** | `Pt(20)` | 内页主标题（实测 24pt 溢出） |
| 卡片标题 | 18pt | `Pt(18)` | 卡片区标题 |
| **正文（默认）** | **14pt** | `Pt(14)` | **正文内容（最常用 134 次）** |
| 分段小标题 | 16pt | `Pt(16)` | 无图分段-N 项的 3/4/5 个分段小标题 |
| 分段正文 | 14pt | `Pt(14)` | 无图分段-N 项的 3/4/5 个分段正文 |
| 编辑器提示 | 14pt | `Pt(14)` | 模板内置说明（最终展示前删除） |
| 模板编辑提示 | 12pt | `Pt(12)` | 编辑区说明（最终展示前删除） |
| 图表标签 | **8-10pt** | `Pt(8)` ~ `Pt(10)` | 图表内文字 |

> **实测说明**：以上字号是 **经 `scripts/preview_html.py` + Playwright 浏览器核验**后调整过的"安全值"。原始规范里 32/36pt 在模板自带的 3.87×0.54 寸标题框里装不下 12+ CJK 字,会溢出/被裁。
>
> 如果你做的是**单行短标题**演示稿,仍可手动调高;但默认严格按本表。

**规则**
- 正文一律 16pt
- 图表标签 8-10pt
- **代码生成时 `font.size` 必须显式设置为 `Pt()` 值，不可为 None**

---

## 四、颜色

模板颜色通过**主题色**（Scheme Colors）统一控制。

### 4.1 主题色引用

| 主题色角色 | 典型用途 | 实测十六进制 | python-pptx |
|---|---|---|---|
| `ACCENT_1` (5) | 辅助灰一 | `#E6E6E5` | `MSO_THEME_COLOR_INDEX.ACCENT_1` |
| `ACCENT_2` (6) | 辅助灰二 | `#ABABAB` | `MSO_THEME_COLOR_INDEX.ACCENT_2` |
| `ACCENT_3` (7) | 主色（兴华红）— 标题、重点色块 | `#C81428` | `MSO_THEME_COLOR_INDEX.ACCENT_3` |
| `ACCENT_4` (8) | 深灰 | `#3E3A39` | `MSO_THEME_COLOR_INDEX.ACCENT_4` |
| `ACCENT_5` (9) | 橙色 | `#ED9027` | `MSO_THEME_COLOR_INDEX.ACCENT_5` |
| `TEXT_1` (13) | 正文文字 | `#3E3A39` | `MSO_THEME_COLOR_INDEX.TEXT_1` |
| `TEXT_2` (15) | 辅助文字 | — | `MSO_THEME_COLOR_INDEX.TEXT_2` |
| `BACKGROUND_1` (14) | 浅底/反白 | `#FFFFFF` | `MSO_THEME_COLOR_INDEX.BACKGROUND_1` |

> **注意**：`MSO_THEME_COLOR_INDEX` 的导入路径是 `pptx.enum.dml.MSO_THEME_COLOR_INDEX`。旧版文档中的 `MSD_THEME_COLOR_INDEX` 为笔误，已更正。

### 4.2 硬编码 RGB 兜底

| 颜色 | 用途 |
|---|---|
| `#FFFFFF` | 深色底反白 |
| `#445469` | 模板编辑提示（最终展示前**必须删除**） |

### 4.3 规则

- **优先使用主题色引用**，跟随模板主题自动切换
- 代码生成时 `font.color` 必须显式设置
- `#445469` 仅用于模板编辑提示，**最终展示前全部删除**

---

## 五、行间距

- **全文统一 1.3**（含正文、标题、图表标签）
- python-pptx：`paragraph.line_spacing = 1.3`

---

## 六、母版版式（111 个，选常用 12 个）

模板提供 111 个版式。下表是从 `template_spec.json` 提取的高频实用版式，**同事只在这 12 个里选，禁止全篇统一版式**。

| 类别 | 版式名 | 适用 |
|---|---|---|
| 封面 | `主题-封面` | 演示封面（详见 §6.3） |
| 目录 | `主题-目录页` | 目录/导航 |
| 章节标题 | `标题页-空白` / `1_标题页-空白` | 章节封面 |
| 过渡 | `1_主题-过渡页` | 章节间过渡 |
| 纯文字 | `文字模板1` ~ `文字模板25` | 正文（按需选） |
| 段落 | `无图分段-3项` / `-4项` / `-5项` | 多项并列 |
| 图文 | `有图分段式-1` ~ `-25` | 图文混排 |
| 逻辑 | `逻辑-2` ~ `-18` | 流程/逻辑图 |
| 图表 | `图表-1` ~ `-8` | 数据图表 |
| 架构 | `架构图-2` ~ `-9` | 组织/系统架构 |
| 图片 | `纯图片` | 全幅图片 |
| 封底 | `自定义版式` (推荐) / `主题-封底页` (简版) | 结束致谢 |

### 6.1 标准页面流程

| 位置 | 必选版式 |
|---|---|
| 第 1 页（封面） | `主题-封面` |
| 第 2 页（目录） | `主题-目录页` |
| 章节首页 | `标题页-空白` |
| 章节过渡 | `1_主题-过渡页` |
| 正文 | `无图分段-*` / `有图分段式-*` / `文字模板*` / `逻辑-*` / `图表-*` / `架构图-*` 任选 |
| 倒数第 2 页（感谢） | `纯图片` 或 `标题页-空白` |
| 最后 1 页（结束） | `自定义版式` (推荐) |

### 6.2 封底（自定义版式）

使用 `自定义版式`（layout #111）。关键要点：
- 标题占位符 idx=11 需从模板默认的 (3.94, 3.01)in 上移到 (3.94, 2.50)in，避免与 slogan 文本框重叠
- 需在 slide 层补加 logo 图片和联系方式文本框
- 联系方式字号 16pt、白色
- 不要写装饰文字（layout 内置）

> 完整逐像素规范、表格和示例代码见 **[docs/layout-spec.md §封底](docs/layout-spec.md#封底自定义版式)**

### 6.3 封面（主题-封面）

使用 `主题-封面` 版式。关键要点：
- 主标题 66pt bold，与水印 "BEIJING XINGHUA" 产生 0.72in 视觉重叠
- 副标题 18pt，强制单行（避免换行）
- 不要 add logo 或写水印文字（layout 已内置）

> 完整逐像素规范、表格和示例代码见 **[docs/layout-spec.md §封面](docs/layout-spec.md#封面主题-封面)**

### 6.4 反模式

- ❌ 全篇只用一种版式（最常见违规）
- ❌ 跳用版本号未确认的版式（`文字模板26+` 未在模板中出现，可能不存在）
- ❌ 用 `6_空白版式` 来自定义内容（违反"用模板版式"原则）
- ❌ 封面标题 < 60pt（会脱离水印 失去视觉重叠）
- ❌ 封面副标题 > 20pt（会被强制换行成 2 行）

---

## 七、占位符 B/J/X/H（已弃用）

**重要更新**：原 165 页模板的 B/J/X/H 占位符体系在 2026 精简版中**已不再使用**。当前模板用 111 个具名版式取代了 B/J/X/H 的占位含义。

**规则**
- 同事不需要再写"页面类型 B/J/X/H"
- 改用"页面版式名"表达页面结构
- 如果同事从旧 PPT 复制内容，**必须人工删除所有 B/J/X/H 孤立标记**

---

## 八、必检项（最终展示前）

- [ ] 画布 13.333" × 7.5"
- [ ] 中文用微软雅黑，英文/数字用 Arial
- [ ] 正文 16pt、图表标签 8-10pt
- [ ] 行间距 1.3
- [ ] 颜色引用主题色（无硬编码 RGB 杂色）
- [ ] 全篇至少使用 **3 种** 不同版式
- [ ] 每页至少 1 个视觉元素（图/表/图标/色块）
- [ ] 无 B/J/X/H 孤立字母
- [ ] 无 `#445469` 灰色模板提示文
- [ ] 无"此处可编辑内容"等占位文

---

## 九、工具脚本（scripts/）

| 脚本 | 用途 | 调用方式 |
|---|---|---|
| `dump_template_spec.py` | 读模板，输出 JSON 规范（**一次性**） | `python dump_template_spec.py <template.pptx> <out.json>` |
| `from_template.py` | 从模板复制出干净 .pptx（按版式清单选页） | `python from_template.py <template.pptx> <out.pptx> <layout1,layout2,...>` |
| `from_outline.py` | **MD 大纲 → .pptx**（核心） | `python from_outline.py outline.md out.pptx [template.pptx]`；紧凑模式加 `--no-chapter-covers` |
| `qa.py` | 自检：字体/字号/颜色 None、版式重复、BJH 残留、提示文、占位符空 | `python qa.py <file.pptx>` |
| `preview_html.py` | **浏览器核验**：把每页 idx/位置/字号/溢出估算渲成 HTML | `python preview_html.py <file.pptx>` |

依赖：`python-pptx >= 1.0.0`、`lxml`、`markitdown`（可选，BJH 残留检查）。

> **新增 `preview_html.py` 的原因**：QA 脚本只能查"格式合规"(字体/字号不是 None、占位符不空),**没法告诉你"标题真的能装进框里不被裁掉"**。preview_html.py 会打开 .pptx,逐页列出每个占位符的 idx + 文本 + 框尺寸 + 字号,并用粗估计算每个文本是否溢出(>框容许行数 → 红色告警)。**生成 HTML 后,双击就能在浏览器逐页核验**,比打开 20MB 的 .pptx 更快。

---

## 十、标准工作流

### 10.1 推荐：MD 大纲 → .pptx

1. 同事在 `outline.md` 写大纲（用 H1/H2/H3 表达层级，详见 `examples/`）
2. 调用 `python scripts/from_outline.py outline.md out.pptx`
3. 调用 `python scripts/qa.py out.pptx` 自检（**格式合规**）
4. 调用 `python scripts/preview_html.py out.pptx` 生成浏览器预览（**视觉估算**）
5. 双击 `out.preview.html`,逐页检查有没有红色 `[溢出!]` 告警
6. 有溢出的话:缩短标题文本 / 换版式 / 在 PowerPoint 里手动缩小字号,回到第 2 步重跑
7. 在 PowerPoint 中打开 `out.pptx`,人工微调版式 / 配色 / 图表
8. 再次运行 `qa.py` 确认无回归
9. 交付

### 10.2 备选：模板复制法

适用场景：同事已有参考页、希望快速起手

1. 在原模板上选定 5-8 个想用的版式（参考 6.1 表）
2. 调用 `python scripts/from_template.py 北京兴华模板.pptx out.pptx "主题-封面,主题-目录页,文字模板1,..."`
3. 在 PowerPoint 中打开，逐页替换文字
4. 运行 `qa.py` 确认

### 10.3 编辑已有 .pptx

1. 运行 `qa.py` 当前状态
2. 在 PowerPoint 中调整
3. 调整后再跑 `qa.py`，对比前后

---

## 十一、禁止事项

- ❌ 脱离模板母版自行配色 / 改字体 / 改画布尺寸
- ❌ 混用宋体、黑体、楷体、Times New Roman、Calibri
- ❌ 纯文字页（无任何视觉元素）
- ❌ 行间距不统一
- ❌ 全篇使用同一种版式
- ❌ 保留 B/J/X/H 孤立字母（从旧模板复制来的话）
- ❌ 保留 `#445469` 灰色编辑提示
- ❌ `font.size` / `font.name` / `font.color` 为 None

---

## 十二、参考

> **注意**：以下路径为原始作者的本地路径，读者需替换为自己的实际路径。
> 模板路径可通过 `BJXH_TEMPLATE` 环境变量、`scripts/config.json` 或 CLI 参数配置。

- **母版文件**：`C:\工作\04-总结与报告\2026年工作\2026合伙人大会\5.27定稿-北京兴华标准化模板20260514修改-5.27修改客户.pptx`（2026-05-27 定稿，59 页，80 个版式）
- **示例 PPT**：`C:\工作\Disorderly\6-28-PPT\北京兴华-AI驱动的审计变革6-16_minimal_prompt_card.pptx`（本地路径）
- **规范数据**：`template_spec.json`（自动生成）
- **版式实测数据**：`docs/layout-spec.md`
- **示例大纲**：`examples/`

---

*版本 v1.1 · 2026-06-18 · 基于 2026 合伙人大会模板 · 修正主题色与默认版式映射*
